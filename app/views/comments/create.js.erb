<% if @comment.commentable_type === 'Post' %>
	$('.post-comments').prepend("<%= j render @comment %>");
	$('form').trigger("reset");
<% else %>
	$('.comment-comments').prepend("<%= j render @comment %>");
	$('form').trigger("reset");
	$('.collapsible').collapsible();
<% end %>